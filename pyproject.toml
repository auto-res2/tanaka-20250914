[project]
name = "star_diffusion_benchmark"
version = "0.1.0"
requires-python = ">=3.9"
description = "Re-structured benchmark for STaR-Diffusion (speed / quality trade-off)."
authors = [
  {name = "OpenAI Auto-Refactor", email = "noreply@openai.com"}
]

# Flat list of required third-party packages (array style as mandated)
dependencies = [
  "torch>=1.9.0",
  "diffusers>=0.19.0",
  "accelerate>=0.20.0",
  "torchmetrics>=0.11.4",
  "ptflops>=0.7",
  "numpy>=1.21.0",
  "matplotlib>=3.5.0",
  "seaborn>=0.11.2",
  "tqdm>=4.64.0",
  "PyYAML>=6.0",
  "requests>=2.28.0"
]
